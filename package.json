{
  "name": "v-scroll-web-components",
  "version": "1.0.0",
  "type": "module",
  "description": "Vite Web Components project with v-scroll custom component",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
